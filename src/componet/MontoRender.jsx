import React from "react";

export default function MontoRender({ monto, moneda }) {
  // en esta seccion se calcula la previsualizacion del monto
  // si es en dolares

  // console.log(monto, moneda);

  return <>MontoRender</>;
}
